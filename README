This example project shows how to implement client-side pagination using ember.js

View it online at [http://toranb.github.io/ember-pagination-example/](http://toranb.github.io/ember-pagination-example/).

The example is 100% server agnostic, just drag the index.html page into your browser

To run the jasmine tests

1. npm install
2. node node_modules/jasmine-phantom-node/bin/jasmine-phantom-node example/static
